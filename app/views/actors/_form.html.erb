<%= form_with(model: actor) do |f| %>
  <% if actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% actor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :full_name, "Full Name" %>
    <%= f.text_field :full_name, class: "form-control form-control-sm" %>
  </div>

  <div class="form-group">
    <%= f.label :image, "Select Image" %>
    <%= f.file_field :image, class: "form-control-file form-control-sm" %>
  </div>

  <%= link_to "Back", movies_path, class: "btn btn-danger btn-sm" %>
  <%= f.submit "Submit", class: "btn btn-primary float-right btn-sm" %>
<% end %>