<% content_for :title, "Edit Movie" %>

<div class="row mt-4">
  <div class="col-12 col-sm-8">
    <!-- Content -->
    <%= render 'form', movie: @movie %>
  </div>

  <!-- Right Sidebar -->
  <%= render 'layouts/sidebar' %>
</div>